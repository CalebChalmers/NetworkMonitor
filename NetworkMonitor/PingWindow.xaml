<Window x:Class="NetworkMonitor.PingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkMonitor"
        xmlns:properties="clr-namespace:NetworkMonitor.Properties"
        mc:Ignorable="d"
        Title="Ping" Height="160" Width="300" WindowStyle="ToolWindow" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="lbx_addresses">
        </ListBox>
        <TextBox x:Name="tbx_address" Grid.Row="1" MaxLines="1">
            
        </TextBox>
        <Grid DockPanel.Dock="Right" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btn_add" Margin="4" Content="Add" Grid.Column="0" Click="btn_add_Click"/>
            <Button x:Name="btn_remove" Margin="4" Content="Remove" Grid.Column="1" Click="btn_remove_Click" IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>
